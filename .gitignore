build/
buildServer.json
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/

# Raw simulator recordings kept locally as capture sources; the web-sized copies
# that the site actually serves live in site/assets/video/.
docs/landing/captures/*.mp4
